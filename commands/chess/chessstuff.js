
const winston = require(`../../logger.js`).winston;
const db = require(`../../logger.js`).db;

function Betrag(zahl){
	if(zahl < 0){
		zahl *= -1;
	}
	return zahl;
}

function validMove(x, y, desx, desy, board){

// var board = db.get('chess.${boardname}').value();
// if (board == undefined) {
// 	winston.error('Chess logic error: boardname undefined/board not in db or correctly referenced');
// 	return;
// }


	switch (expression) {
		case expression:

			break;
		default:

	}
}
